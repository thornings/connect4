export default class Player {
    name = "";
    color = "player-one";
    
    constructor(name, color, sign) {
        this.name = name;
        this.color = color;
        this.sign = sign;
    }

    // Color() {
    //     return this.color;
    // }
}